# Development-history archive

These files are retained for provenance because they document intermediate model-development experiments that preceded the final TBI-TRACT specification. They are **not** part of the primary manuscript reproduction sequence.

The final predictor policies and deployment models are defined by the numbered scripts in `01_cohort/` through `05_reporting/`. Results from files in this archive should not be interpreted as alternative final models.
