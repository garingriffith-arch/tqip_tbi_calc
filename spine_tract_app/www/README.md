# Static assets

Optional static files for the SPINE-TRACT Shiny app.

Recommended optional file:

```text
ohsu_logo.png
```

If `ohsu_logo.png` is present, the app header will render it automatically. Otherwise, it displays an OHSU text fallback.
